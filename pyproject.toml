[project]
name = "stats-scraper-backend"
version = "0.1.0"
description = "LLM proxy backend for Analytics Idea Lab"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
    "google-genai>=1.0.0",
    "mistralai>=1.0.0",
]

[tool.uv]
dev-dependencies = []
