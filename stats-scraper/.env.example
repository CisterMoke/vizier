# Backend API URL the frontend connects to.
# For local development: http://localhost:8000
# For nginx deployment (systemd setup): /api (relative path, proxied by nginx)
VITE_BACKEND_URL=http://localhost:8000
